# KLSI Frontend

This folder contains the frontend application built with Vite + React.

Development
- Install dependencies: `npm install`
- Start dev server: `npm run dev`

Production
- Build: `npm run build`
- Serve with docker: `docker build -f Dockerfile.production -t klsi-frontend .` and run.
